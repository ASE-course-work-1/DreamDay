@model DreamDay.Models.Vendor
  @{
      ViewData["Title"] = "Create Vendor";
  }
  <div class="row justify-content-center">
      <div class="col-md-6">
          <div class="card">
              <div class="card-header bg-primary text-white">
                  <h4><i class="fas fa-plus me-2"></i>Create Vendor</h4>
              </div>
              <div class="card-body">
                  <form asp-action="Create" method="post" class="mt-3">
                      <div class="mb-3">
                          <label asp-for="Name" class="form-label"><i class="fas fa-store me-2"></i>Name</label>
                          <input asp-for="Name" class="form-control" required />
                          <span asp-validation-for="Name" class="error-message"></span>
                      </div>
                      <div class="mb-3">
                          <label asp-for="Type" class="form-label"><i class="fas fa-tag me-2"></i>Type</label>
                          <input asp-for="Type" class="form-control" required />
                          <span asp-validation-for="Type" class="error-message"></span>
                      </div>
                      <div class="mb-3">
                          <label asp-for="Price" class="form-label"><i class="fas fa-dollar-sign me-2"></i>Price</label>
                          <input asp-for="Price" class="form-control" type="number" step="0.01" required />
                          <span asp-validation-for="Price" class="error-message"></span>
                      </div>
                      <div class="mb-3">
                          <label asp-for="Reviews" class="form-label"><i class="fas fa-star me-2"></i>Reviews</label>
                          <textarea asp-for="Reviews" class="form-control"></textarea>
                          <span asp-validation-for="Reviews" class="error-message"></span>
                      </div>
                      <button type="submit" class="btn btn-primary w-100"><i class="fas fa-check me-2"></i>Create</button>
                  </form>
              </div>
          </div>
      </div>
  </div>